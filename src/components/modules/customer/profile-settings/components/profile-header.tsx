import {
  HeaderContent,
  ProfileHeaderContentProps,
} from "./header/header-content";

export default function ProfileHeader(props: ProfileHeaderContentProps) {
  return <HeaderContent {...props} />;
}
